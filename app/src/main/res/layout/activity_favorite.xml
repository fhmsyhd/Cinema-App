<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/top_app_bar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@android:color/transparent"
        android:paddingHorizontal="8dp"
        app:menu="@menu/action_menu_favorite_library"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationIconTint="@color/cinema_text_primary"
        app:title="@string/my_favorites"
        app:titleTextColor="@color/cinema_text_primary" />

    <TextView
        android:id="@+id/tv_favorite_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingTop="4dp"
        android:paddingEnd="20dp"
        android:paddingBottom="10dp"
        android:textColor="@color/cinema_text_secondary"
        android:textSize="14sp" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_favorite_movies"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingHorizontal="12dp"
            android:paddingBottom="24dp" />

        <include
            android:id="@+id/loading_favorites"
            layout="@layout/view_loading_favorites"
            android:visibility="gone" />

        <include
            android:id="@+id/view_empty"
            layout="@layout/view_empty"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
